generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model VrmModel {
  id        Int      @id @default(autoincrement())
  name      String
  path      String
  icon      String
  createdAt DateTime @default(now())
}
